<% deal_id = @deal.id %>
<%= form_for @deal.attendances.build(:user_id => current_user.id) do |f| %>
<div><%= f.hidden_field :deal_id %></div>
<div class="actions"><%= f.submit "Buy", :class => "attendbox" %></div>
<% end %>
